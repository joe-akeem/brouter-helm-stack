apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brouter.fullname" . }}-test-connection"
  labels:
    {{- include "brouter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brouter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
